#
# Sigstore attachment configuration for ghcr.io/isaiahaguilera/eaglesnest-os.
# Needed so rpm-ostree/bootc can locate cosign signatures during updates.
#
docker:
  ghcr.io/isaiahaguilera/eaglesnest-os:
    use-sigstore-attachments: true
